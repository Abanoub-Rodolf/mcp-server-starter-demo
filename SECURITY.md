# Security

This demo is intentionally stdio-only and exposes two in-memory tools. It does not read files, make network requests, spawn subprocesses, or accept credentials.

Report vulnerabilities privately through [ThynkQ](https://thynkq.com/security). Do not include secrets or private configuration in a public issue.
